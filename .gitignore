# Dependencies
node_modules/

# Build output
build/
dist/

# TypeScript
*.tsbuildinfo

# Logs
*.log
npm-debug.log*

# OS files
.DS_Store
Thumbs.db

# IDE
.idea/
.vscode/
*.swp
*.swo

# Environment
.env
.env.local

# OCR trained data files (legacy, no longer used)
*.traineddata

# Git worktrees
.worktrees/
.claude/worktrees/

# Claude Code harness-generated files
.claude/scheduled_tasks.lock
# MCP Registry DNS auth signing key — permanent credential for the
# com.execbro/* namespace. Never commit; rotating it requires deleting the
# old apex TXT record on execbro.com or verification fails.
*.pem
mcp-registry-key.hex

# Build-hygiene denylist names a real competitor — kept local, never committed.
# See scripts/build-hygiene-denylist.example.json for the shape.
scripts/build-hygiene-denylist.json
